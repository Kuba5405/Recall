# Environment variable template for Recall.
# Copy to .env (which is git-ignored) and fill in real values locally.
# Real secrets must never be committed — placeholders only in this file.
#
# No configuration exists yet; this file will be populated once the tech
# stack and environment strategy are decided in Phase 2 (Architecture).
